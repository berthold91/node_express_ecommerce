{
  "name": "frontend",
  "version": "1.0.0",
  "description": "this will transform the frontend of our project into a npm project",
  "main": "index.js",
  "scripts": {
    "start": "live-server public --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": ".git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
